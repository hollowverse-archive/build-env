sudo: required

services:
  - docker

script:
  - docker build -t hollowverse/build-env ./
  - if [[ ($TRAVIS_PULL_REQUEST == "false") && ($TRAVIS_BRANCH == "master") ]]; then 
      docker login -u "${DOCKER_USER}" -p "${DOCKER_PASS}" && docker push hollowverse/build-env;
    fi

notifications:
  email: false
